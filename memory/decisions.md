# Decisions

Record decisions that should shape future work.

## 2026-04-26

- Treat `/Users/eli/Downloads/Elizabeth-Codex-Context` as the current master personal AgentOS context project.
- Keep `AGENTS.md` as the boot file.
- Keep `context-portfolio/` as the deep context library.
- Add `agent-os/` as the operating layer.
- Add `skills/`, `memory/`, `connections/`, `agents/`, and `projects/` as first-class OS areas.
- Move on from identity/context polishing to first skills.
